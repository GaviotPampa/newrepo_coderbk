export default class ProdReqDto {
  constructor(product) {
    this.product.marca = title;

     this.product.importe = product.title;

     this.product.descipcion= product.description;

     this.product.stock= product.stock;

     this.product.estado= product.status;

     this.product.categoria= product.category;

     this.product.codigo= product.code;

     this.product.owner= product.owner;
  };
};
